# README

`test.c` is used to generate `rand_double.txt`, `rand_int.txt`, and `rand_string.txt`.

Use

```bash
clang -std=gnu11 -O2 -Wall -Wextra -pedantic -Wno-unused-result -Wconversion -fno-omit-frame-pointer -o h3 h3.c forward_list.c
```

to generate the executable file.